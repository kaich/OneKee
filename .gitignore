# 本仓库仅用于发布产物和文档，不应包含构建中间产物
# 必须使用 GitHub Release 上传二进制，禁止把二进制 commit 进仓库

# 构建产物
/dist/
/target/
/build/

# 本地下载缓存
*.dmg
*.exe
*.msi
*.AppImage
*.deb
*.rpm
*.apk
*.aab
*.ipa
*.zip
*.tar.gz
*.tgz

# CLI 二进制（按命名约定，锚定根目录，避免误伤 skills/onekee-cli）
/onekee
/onekee-*
/onekee.exe

# 校验文件（随 Release 上传，不进仓库）
SHA256SUMS
SHA256SUMS.asc
*.sha256

# 编辑器/系统文件
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
